name : Test Workflow

on:
  push:
    branches:
      - main

jobs:
  working-job:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Message
        run: echo "GitHub Actions working Successfully ✅!"

      